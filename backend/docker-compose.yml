version: '3.8'

services:
  web:
    container_name: backend
    build: .
    env_file:
      - ./app/.env
    ports:
      - "8000:80"
    depends_on:
      - redis
  redis:
    container_name: backend_redis
    image: "redis:alpine"
    ports:
      - "6379:6379"
